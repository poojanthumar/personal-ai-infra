FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        build-essential \
        ca-certificates \
        curl \
        ffmpeg \
        git \
        openssh-client \
        postgresql-client \
        python3 \
        python3-pip \
        python3-venv \
        python3.12-venv \
        zstd \
    && rm -rf /var/lib/apt/lists/*

USER ubuntu
